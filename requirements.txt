# Runtime dependencies for jy.
#
# tkinter is part of the standard library but ships separately on most Linux
# distributions; install it with the system package manager, not pip:
#
#   Debian/Ubuntu   sudo apt install python3-tk
#   Fedora          sudo dnf install python3-tkinter
#   macOS/Windows   included with python.org builds

ruamel.yaml>=0.17,<1.0
