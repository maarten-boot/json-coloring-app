[pytest]
# The gui marker is registered in conftest.py as well, so it works even when this file is not picked up -
# a flat checkout, or pytest started from another directory.
markers =
    gui: needs a display; builds a real Tk application
filterwarnings =
    error::DeprecationWarning
